# App environment
# Set to 'development' locally or 'production' in deployments
NODE_ENV=development

# Public base URL used to build absolute URLs (images, links)
# In development the app defaults to http://localhost:3000 when unset
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database (PostgreSQL) — required by Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public&connection_limit=5&pool_timeout=30
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nicotordev?schema=public&connection_limit=5&pool_timeout=30

# DeepL API key — required for scripts/auto-translate.ts
# Get one at https://www.deepl.com/pro-api
DEEPL_API_KEY=  

# Optional: disable Next.js telemetry (already set in npm scripts)
# NEXT_TELEMETRY_DISABLED=1

AUTH_SECRET="Scp3bWIyQbCtM2yokZ6u5M7ePlmUb19syRSVcawke2c=" # Added by `npx auth`. Read more: https://cli.authjs.dev
